#include <iostream> 
#include <vector> 
 
using namespace std; 

int main() {
    int n;
    scanf("%d", &n);
    vector<int> num; 
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &num[i]);
    }
    
    return 0;
}